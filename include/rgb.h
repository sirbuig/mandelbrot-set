#ifndef MANDELBROT_SET_RGB_H
#define MANDELBROT_SET_RGB_H

typedef struct {
    unsigned char blue;
    unsigned char green;
    unsigned char red;
} RGB; // in bitmap the order is blue, green, red

#endif //MANDELBROT_SET_RGB_H
